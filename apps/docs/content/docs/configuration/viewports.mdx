---
title: Viewports
description: Single and multi-viewport setup
---

Test across different screen sizes to ensure your UI works on various devices.

## Configuration

```ts
// visnap.config.ts|js
viewport: {
  desktop: { width: 1920, height: 1080 },
  tablet: { width: 768, height: 1024 },
  mobile: { width: 375, height: 667 },
}
```

<ApiTable
  title="Viewport Map"
  caption="Define one or more named viewport sizes. Keys are free-form strings."
  rows={ApiTable.defineRows([
    {
      name: "viewport",
      type: "Record<string, { width: number; height: number; deviceScaleFactor?: number }>",
      description:
        "A map of named viewports used across captures unless overridden per test.",
    },
  ])}
  anchorPrefix="viewport"
/>

## Single Viewport

Test with one viewport size by providing a single key in the map.

## Multiple Viewports

Test across multiple screen sizes by defining multiple named entries.

## Per-Test Viewports

Override viewports for specific test cases using adapter-specific configuration:

- **Storybook**: Use `parameters.visualTesting.viewport`
- **URL Adapter**: Use `viewport` property in URL configuration

See the specific adapter documentation for details.
